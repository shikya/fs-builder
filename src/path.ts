/**
 * Path Class
 * It will contain the general path in string array.
 * Starting from 0th index as a root.
 */
export class Path {
    /**
     * directories to the path
     * starting with 0th as root
     */
    public directory: string[];
}